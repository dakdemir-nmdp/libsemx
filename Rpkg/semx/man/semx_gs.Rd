% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs.R
\name{semx_gs}
\alias{semx_gs}
\title{Genomic Selection Model}
\usage{
semx_gs(
  formula,
  data,
  geno_id,
  markers = NULL,
  kernel = NULL,
  estimator = "reml",
  family = "gaussian",
  ...
)
}
\arguments{
\item{formula}{A formula specifying the fixed effects (e.g., `y ~ 1 + cov1`).}

\item{data}{A data frame containing the variables in the formula.}

\item{geno_id}{Character string specifying the column in `data` that links observations to genotypes.}

\item{markers}{A numeric matrix of markers (rows = genotypes, cols = markers). Row names must match `geno_id` levels.}

\item{kernel}{Optional pre-computed relationship matrix (GRM). If provided, `markers` is ignored.}

\item{estimator}{Estimation method: "reml" (default) or "ml".}

\item{family}{Outcome distribution family (default: "gaussian").}

\item{...}{Additional arguments passed to `semx_fit`.}
}
\value{
A `semx_fit` object.
}
\description{
Fits a genomic selection model (GBLUP) using a mixed model framework.
}
