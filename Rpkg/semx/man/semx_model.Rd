% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/semx.R
\name{semx_model}
\alias{semx_model}
\title{Build a ModelIR from high-level equations}
\usage{
semx_model(
  equations = NULL,
  families = NULL,
  kinds = NULL,
  covariances = NULL,
  genomic = NULL,
  random_effects = NULL,
  parameters = NULL,
  data = NULL
)
}
\arguments{
\item{equations}{Character vector of equations. Blank entries are ignored.}

\item{families}{Named character vector that maps observed variables to outcome
family identifiers such as ``gaussian`` or ``binomial``.}

\item{kinds}{Optional named character vector forcing select variables to be
``observed``, ``latent``, or ``grouping``.}

\item{covariances}{Optional list of covariance definitions (name, structure, dimension).}

\item{genomic}{Optional named list of genomic covariance specs; each entry should provide
a numeric matrix `markers` and optional `structure`, `center`, and `normalize` flags.}

\item{random_effects}{Optional list of random effect definitions (name, variables, covariance).}

\item{parameters}{Optional named list of initial values for parameters.}

\item{data}{Optional list or data.frame containing data. If provided, it can be used to resolve
genomic matrices referenced in `genomic` specs or equations.}
}
\value{
A list with class \code{semx_model} that contains the compiled
  \code{ir}, \code{variables}, and \code{edges} components.
}
\description{
This helper parses simple lavaan-style equations (\code{=~}, \code{~}, \code{~~})
and emits a \code{ModelIR} object plus lightweight metadata lists.
}
